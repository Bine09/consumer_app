<% if signed_in? %>
  <div class="container center_div form-group row">
  <br>


<% @newcomment = @product.comments.build %>

  <%= form_for([@product, @newcomment]) do |f| %>
  <% if @newcomment.errors.any? %>
     <div id="error_explanation">
      <h2 class= 'alert-light' ><%= pluralize(@newcomment.errors.count, "error")%> prohibited this comment from being saved:</h2>


      <% @newcomment.errors.full_messages.each do |message| %>
        <p><%= message %></p>
      <% end %>


  <% end %>
        <p><h3>Add a review</h3>
        <%= f.label :body, "Your Comment", class:'col col-form-label text-white' %><br>
        <%= f.text_area :body, class:'form-control form-control-sm'%>
        <p> <div class="rating"></div>
        <p>
        <p> <%= f.submit "Submit", class: 'btn btn-info' %>

    </div>
  <% end %>


<% else %>
 <h5> You need to be logged in, in order to write a comment!</h5>
<% end %>
